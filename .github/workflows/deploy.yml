name: Terraform Infra Deploy

on:
  push:
    branches: ["main"]
    paths:
      - infra/**
      - .github/workflows/deploy.yml
      - infra/build.sh
  pull_request:
    branches: ["main"]
    paths:
      - infra/**
      - .github/workflows/deploy.yml
      - infra/build.sh
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1

concurrency:
  group: terraform-infra-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    name: Detect changed infra layers
    runs-on: ubuntu-latest
    outputs:
      layers: ${{ steps.detect.outputs.layers }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Detect changed layers
        id: detect
        shell: bash
        run: |
          set -euo pipefail

          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BASE_SHA="${{ github.event.pull_request.base.sha }}"
            HEAD_SHA="${{ github.sha }}"
          else
            BASE_SHA="${{ github.event.before }}"
            HEAD_SHA="${{ github.sha }}"
          fi

          layers_json="$(bash infra/build.sh detect-layers "${BASE_SHA}" "${HEAD_SHA}")"
          echo "layers=${layers_json}" >> "$GITHUB_OUTPUT"

  deploy:
    name: Terraform Deploy
    needs: changes
    if: ${{ needs.changes.outputs.layers != '[]' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        layer: ${{ fromJSON(needs.changes.outputs.layers) }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure AWS credentials (OIDC)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3

      - name: Terraform Plan
        run: bash infra/build.sh terraform-plan "${{ matrix.layer }}"

      - name: Terraform Apply
        if: github.ref == 'refs/heads/main' && github.event_name == 'push'
        run: bash infra/build.sh terraform-apply "${{ matrix.layer }}"
